# Power BI export note
Pre-aggregated dashboard-page CSVs are included. The larger `fact_feedback.csv` is omitted from the GitHub-ready package to keep the repository lightweight; run `scripts/09_powerbi_export.py` after regenerating the full pipeline to recreate it.
