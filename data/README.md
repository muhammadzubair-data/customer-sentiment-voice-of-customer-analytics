# Data note
The repository keeps compact samples and metadata rather than the largest generated CSVs. Run scripts 01–07 to regenerate the complete synthetic datasets. `sample_feedback_scored_5000.csv` is a 5,000-row scored sample for inspection. Full project-scale counts and verified publication metrics are recorded in `outputs/project_metrics.json` and `PUBLICATION_AUDIT.md`.
